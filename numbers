#PHP provides automatic data type conversion.


#If you assign an integer value to a variable, the type of that variable will automatically be an integer.

#Then, if you assign a string to the same variable, the type will change to a string.


#This automatic conversion can sometimes break your code.

< ?PHP

$x = 5985;

var_dump(is_int($x));

$x = 59.85;

var_dump(is_int($x));

?>
